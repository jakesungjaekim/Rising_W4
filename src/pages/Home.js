import React from "react";

import { Fragment } from "react";
import Button from "../components/UI/Button";

const Home = (props) => {
  return (
    <Fragment>
      <Button>로그인</Button>
    </Fragment>
  );
};

export default Home;
